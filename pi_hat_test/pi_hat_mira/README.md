# pi_hat_mira

This is an initial port of BZ `mira_test` firmware to the custom Pi hat board.
_Only source files_ are to be kept here!

After loading into radiant, all the IP cores need to be updated and regenerated.

The idea is that only source edits should be put back into this repo, and
the design should not be built here and pushed.

